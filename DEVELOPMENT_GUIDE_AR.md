
# 📚 دليل التطوير والتعديل الشامل - موقع جمعية يعمر

## 📋 جدول المحتويات

1. [نظرة عامة على المشروع](#نظرة-عامة)
2. [البدء السريع](#البدء-السريع)
3. [هيكل المشروع](#هيكل-المشروع)
4. [شرح الملفات الرئيسية](#شرح-الملفات-الرئيسية)
5. [كيفية التعديل](#كيفية-التعديل)
6. [إضافة محتوى جديد](#إضافة-محتوى-جديد)
7. [التخصيص والتعديلات](#التخصيص-والتعديلات)
8. [نشر الموقع](#نشر-الموقع)
9. [حل المشاكل الشائعة](#حل-المشاكل)

---

## 🎯 نظرة عامة على المشروع {#نظرة-عامة}

### التقنيات المستخدمة

```
📦 Next.js 15.2 (Page Router)
⚛️  React 19
🎨 Tailwind CSS v3
📘 TypeScript
🧩 Shadcn/UI Components
✨ Framer Motion (للحركات)
🎭 Lucide React (للأيقونات)
```

### الميزات الرئيسية

- ✅ تصميم متجاوب (Responsive) - يعمل على جميع الأجهزة
- ✅ دعم كامل للغة العربية (RTL)
- ✅ حركات وتأثيرات بصرية متقدمة
- ✅ عدادات متحركة للإنجازات
- ✅ سلايدر أخبار أوتوماتيكي
- ✅ زر واتساب عائم دائم
- ✅ أداء عالي وسرعة تحميل ممتازة

---

## 🚀 البدء السريع {#البدء-السريع}

### المتطلبات

```bash
Node.js 18.0 أو أحدث
npm أو yarn
```

### تشغيل المشروع محلياً

```bash
# 1. تثبيت المكتبات
npm install

# 2. تشغيل السيرفر المحلي
npm run dev

# 3. فتح المتصفح على
http://localhost:3000
```

### أوامر مفيدة أخرى

```bash
# بناء المشروع للإنتاج
npm run build

# تشغيل النسخة المبنية
npm start

# فحص الأخطاء البرمجية
npm run lint

# إصلاح الأخطاء تلقائياً
npm run lint -- --fix
```

---

## 📁 هيكل المشروع {#هيكل-المشروع}

```
yaamur-website/
│
├── public/                    # الملفات العامة (صور، خطوط)
│   ├── logo.png              # شعار الجمعية
│   ├── favicon.ico           # أيقونة الموقع
│   └── images/               # مجلد الصور
│       ├── mosques/          # صور المساجد
│       ├── news/             # صور الأخبار
│       └── partners/         # شعارات الشركاء
│
├── src/
│   ├── components/           # المكونات القابلة لإعادة الاستخدام
│   │   ├── ui/              # مكونات shadcn/ui الأساسية
│   │   ├── AnimatedCounter.tsx      # عداد متحرك للأرقام
│   │   ├── NewsSlider.tsx           # سلايدر الأخبار
│   │   ├── PartnersCarousel.tsx     # عرض الشركاء
│   │   ├── TestimonialsSection.tsx  # قسم الشهادات
│   │   ├── WhatsAppButton.tsx       # زر الواتساب العائم
│   │   ├── MobileMenu.tsx           # قائمة الموبايل
│   │   ├── ScrollReveal.tsx         # تأثير الظهور عند التمرير
│   │   └── ClientOnly.tsx           # للتعامل مع SSR
│   │
│   ├── pages/               # صفحات الموقع (Next.js Pages Router)
│   │   ├── _app.tsx        # الإعدادات العامة للتطبيق
│   │   ├── _document.tsx   # هيكل HTML الأساسي
│   │   ├── index.tsx       # الصفحة الرئيسية ⭐
│   │   └── 404.tsx         # صفحة الخطأ 404
│   │
│   ├── styles/
│   │   └── globals.css     # الأنماط العامة والألوان
│   │
│   ├── lib/
│   │   └── utils.ts        # دوال مساعدة
│   │
│   ├── contexts/           # إدارة الحالة (Context API)
│   │   └── ThemeProvider.tsx
│   │
│   └── hooks/              # React Hooks مخصصة
│       └── use-mobile.tsx
│
├── tailwind.config.ts      # إعدادات Tailwind CSS
├── tsconfig.json          # إعدادات TypeScript
├── next.config.mjs        # إعدادات Next.js
├── package.json           # معلومات المشروع والمكتبات
└── README.md              # معلومات عامة
```

---

## 📄 شرح الملفات الرئيسية {#شرح-الملفات-الرئيسية}

### 1. `src/pages/index.tsx` - الصفحة الرئيسية ⭐

**أهم ملف في المشروع** - يحتوي على جميع أقسام الموقع:

```typescript
// الأقسام الموجودة:
1. Header (الترويسة)           - السطر 35
2. Hero Section (القسم البطل)    - السطر 95
3. Services (الخدمات)          - السطر 200
4. Latest News (آخر الأخبار)   - السطر 290
5. Achievements (الإنجازات)    - السطر 340
6. Testimonials (الشهادات)     - السطر 385
7. Partners (الشركاء)          - السطر 400
8. Join Section (انضم إلينا)   - السطر 450
9. Footer (الذيل)              - السطر 520
```

**كيف تعمل الصفحة؟**

```typescript
export default function HomePage() {
  // 1. إدارة الحالة (State Management)
  const [isScrolled, setIsScrolled] = useState(false);
  const [showScrollTop, setShowScrollTop] = useState(false);

  // 2. تأثيرات التمرير (Scroll Effects)
  useEffect(() => {
    const handleScroll = () => {
      setIsScrolled(window.scrollY > 20);
      setShowScrollTop(window.scrollY > 400);
    };
    window.addEventListener("scroll", handleScroll);
    return () => window.removeEventListener("scroll", handleScroll);
  }, []);

  // 3. إرجاع JSX (HTML بشكل React)
  return (
    <div dir="rtl">
      {/* الأقسام هنا */}
    </div>
  );
}
```

---

### 2. `src/styles/globals.css` - الأنماط والألوان

```css
/* ألوان جمعية يعمر */
:root {
  --brand-deep-green: #08704C;    /* الأخضر الغامق */
  --brand-warm-brown: #7B4F28;    /* البني الدافئ */
  --brand-teal: #00A186;          /* الفيروزي */
  --brand-light-blue: #C3CFF1;    /* الأزرق الفاتح */
  --brand-cream: #F8F4ED;         /* الكريمي (الخلفية) */
}

/* استخدام الألوان في CSS */
.bg-brand-green {
  background-color: var(--brand-deep-green);
}

/* استخدام الألوان في JSX */
<div className="bg-[#08704C]">محتوى</div>
```

**الأنماط الإسلامية الهندسية:**

```css
.pattern-dots     /* نمط نقاط */
.pattern-grid     /* نمط شبكي */
.pattern-diagonal /* نمط قطري */
.pattern-stars    /* نمط نجوم */
```

---

### 3. `src/components/NewsSlider.tsx` - سلايدر الأخبار

```typescript
const newsItems = [
  {
    id: 1,
    title: "افتتاح مسجد الرحمة الجديد",
    description: "بحمد الله تم افتتاح...",
    date: "2025-11-01",
    image: "/images/news/news-1.jpg"  // مسار الصورة
  },
  // أضف المزيد من الأخبار هنا
];
```

**ميزات السلايدر:**

- ✅ حركة أوتوماتيكية كل 5 ثوانٍ
- ✅ يتوقف عند التمرير بالماوس
- ✅ أزرار تنقل يدوية
- ✅ نقاط مؤشرة للشرائح
- ✅ متجاوب مع الموبايل

---

### 4. `src/components/WhatsAppButton.tsx` - زر الواتساب

```typescript
export function WhatsAppButton() {
  const phoneNumber = "966501234567";  // غير هذا الرقم ⚠️
  const message = "السلام عليكم، أرغب في الاستفسار عن خدمات جمعية يعمر";

  const handleClick = () => {
    const url = `https://wa.me/${phoneNumber}?text=${encodeURIComponent(message)}`;
    window.open(url, "_blank");
  };

  return (
    <button onClick={handleClick} className="fixed bottom-8 right-8...">
      {/* زر الواتساب */}
    </button>
  );
}
```

---

### 5. `src/pages/_document.tsx` - إعدادات HTML والخطوط

```typescript
export default function Document() {
  return (
    <Html lang="ar" dir="rtl">  {/* اتجاه من اليمين لليسار */}
      <Head>
        {/* خطوط Google - Cairo و Tajawal */}
        <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@300;400;600;700;900&display=swap" rel="stylesheet" />
      </Head>
      <body>
        <Main />
        <NextScript />
      </body>
    </Html>
  );
}
```

---

## ✏️ كيفية التعديل {#كيفية-التعديل}

### 1. تغيير النصوص والمحتوى

**مثال: تعديل عنوان القسم الرئيسي**

في ملف `src/pages/index.tsx` - ابحث عن السطر ~130:

```typescript
// قبل التعديل:
<h2 className="text-5xl...">
  نعمر
  <span>بيوت الله</span>
  <span>بإتقان وإخلاص</span>
</h2>

// بعد التعديل:
<h2 className="text-5xl...">
  نحن نبني
  <span>المساجد</span>
  <span>بكل تفان</span>
</h2>
```

---

### 2. تغيير الألوان

**خطوات تغيير لون العلامة التجارية:**

**الخطوة 1:** افتح `src/styles/globals.css`

```css
/* ابحث عن السطر ~10 */
:root {
  --brand-deep-green: #08704C;    /* غير هذا اللون */
  --brand-warm-brown: #7B4F28;    /* أو هذا */
  --brand-teal: #00A186;          /* أو هذا */
}
```

**الخطوة 2:** احفظ الملف - التغييرات ستظهر فوراً!

---

### 3. إضافة/تعديل الصور {#تعديل-الصور}

#### أ) رفع الصور للمشروع

**الخطوة 1:** أنشئ المجلدات في `public/`:

```
public/
├── images/
│   ├── mosques/
│   │   ├── mosque-1.jpg
│   │   ├── mosque-2.jpg
│   │   └── mosque-3.jpg
│   ├── news/
│   │   ├── news-1.jpg
│   │   └── news-2.jpg
│   └── partners/
│       ├── partner-1.png
│       └── partner-2.png
```

**الخطوة 2:** استخدم الصور في الكود:

```typescript
// في NewsSlider.tsx
const newsItems = [
  {
    id: 1,
    title: "افتتاح مسجد الرحمة",
    image: "/images/news/news-1.jpg"  // ✅ صحيح
  }
];

// في أي مكون آخر:
<img src="/images/mosques/mosque-1.jpg" alt="مسجد" />
```

#### ب) تغيير الشعار (اللوقو)

**الخطوة 1:** ضع ملف الشعار في `public/logo.png`

**الخطوة 2:** افتح `src/pages/index.tsx` - السطر ~52:

```typescript
// قبل التعديل (أيقونة):
<div className="w-12 h-12 bg-gradient-to-br...">
  <Building2 className="w-7 h-7 text-white" />
</div>

// بعد التعديل (صورة شعار):
<img 
  src="/logo.png" 
  alt="جمعية يعمر" 
  className="w-12 h-12 object-contain"
/>
```

**للشعار الكامل (صورة + نص):**

```typescript
<img 
  src="/logo-full.png" 
  alt="جمعية يعمر لعناية المساجد" 
  className="h-16 w-auto object-contain"
/>
```

---

### 4. تعديل معلومات الاتصال

**في Footer (الذيل) - `src/pages/index.tsx` السطر ~600:**

```typescript
// رقم الهاتف
<span dir="ltr">+966 50 123 4567</span>  // غير هذا

// البريد الإلكتروني
<span>info@yaamur.org</span>  // غير هذا

// العنوان
<span>المنطقة الشرقية، المملكة العربية السعودية</span>  // غير هذا
```

---

### 5. تغيير رقم الواتساب ⚠️ مهم

**افتح `src/components/WhatsAppButton.tsx` - السطر 4:**

```typescript
const phoneNumber = "966501234567";  // غير هذا الرقم

// التنسيق الصحيح:
// 966XXXXXXXXX (كود الدولة + الرقم بدون الصفر)
// مثال: 966555123456
```

---

### 6. إضافة/تعديل الأخبار

**افتح `src/components/NewsSlider.tsx` - السطر 7:**

```typescript
const newsItems = [
  {
    id: 1,
    title: "افتتاح مسجد الرحمة الجديد",
    description: "بحمد الله تم افتتاح مسجد الرحمة...",
    date: "2025-11-01",
    image: "/images/news/news-1.jpg"
  },
  // أضف خبراً جديداً:
  {
    id: 5,  // رقم تسلسلي جديد
    title: "عنوان الخبر الجديد",
    description: "وصف الخبر...",
    date: "2025-11-10",
    image: "/images/news/news-5.jpg"
  }
];
```

---

### 7. تعديل الإنجازات (الأرقام)

**في `src/pages/index.tsx` - السطر ~370:**

```typescript
const stats = [
  { number: 150, label: "مسجد تم بناؤه", suffix: "+", icon: Building2 },
  { number: 500, label: "عملية صيانة", suffix: "+", icon: Wrench },
  { number: 300, label: "مسجد قيد التشغيل", suffix: "+", icon: Sparkles },
  { number: 50, label: "مشروع جاري", suffix: "+", icon: TrendingUp }
];

// غير الأرقام حسب إنجازاتكم الفعلية
```

---

### 8. إضافة شركاء جدد

**افتح `src/components/PartnersCarousel.tsx`:**

```typescript
const partners = [
  { id: 1, name: "شريك 1", logo: "/images/partners/partner-1.png" },
  { id: 2, name: "شريك 2", logo: "/images/partners/partner-2.png" },
  // أضف شريك جديد:
  { id: 6, name: "شريك جديد", logo: "/images/partners/partner-6.png" }
];
```

---

## 🎨 التخصيص المتقدم {#التخصيص-والتعديلات}

### 1. تغيير الخطوط

#### أ) استخدام خط Google Fonts مختلف:

**الخطوة 1:** اختر خطك من [Google Fonts](https://fonts.google.com/?subset=arabic)

**الخطوة 2:** افتح `src/pages/_document.tsx` - السطر 8:

```typescript
<link
  href="https://fonts.googleapis.com/css2?family=اسم-الخط:wght@400;700&display=swap"
  rel="stylesheet"
/>
```

**الخطوة 3:** افتح `tailwind.config.ts` - السطر 15:

```typescript
fontFamily: {
  sans: ["اسم-الخط", "system-ui", "sans-serif"],
  arabic: ["اسم-الخط", "sans-serif"],
}
```

#### ب) استخدام خط مخصص (ملفات woff/woff2):

**الخطوة 1:** ضع ملفات الخط في `public/fonts/`:

```
public/
└── fonts/
    ├── CustomFont-Regular.woff2
    └── CustomFont-Bold.woff2
```

**الخطوة 2:** افتح `src/styles/globals.css` - أعلى الملف:

```css
@font-face {
  font-family: 'CustomArabic';
  src: url('/fonts/CustomFont-Regular.woff2') format('woff2');
  font-weight: 400;
  font-style: normal;
  font-display: swap;
}

@font-face {
  font-family: 'CustomArabic';
  src: url('/fonts/CustomFont-Bold.woff2') format('woff2');
  font-weight: 700;
  font-style: normal;
  font-display: swap;
}
```

**الخطوة 3:** استخدم الخط في `tailwind.config.ts`:

```typescript
fontFamily: {
  sans: ["CustomArabic", "system-ui", "sans-serif"],
}
```

---

### 2. تخصيص الحركات والتأثيرات

**تسريع/تبطيء السلايدر:**

```typescript
// في src/components/NewsSlider.tsx - السطر 26
const interval = setInterval(() => {
  setCurrentIndex((prev) => (prev + 1) % newsItems.length);
}, 5000);  // غير 5000 إلى الوقت المطلوب بالميلي ثانية
// 3000 = 3 ثوانٍ
// 7000 = 7 ثوانٍ
```

**تغيير سرعة عداد الأرقام:**

```typescript
// في src/components/AnimatedCounter.tsx - السطر 7
duration = 2000  // غير 2000 إلى سرعة مختلفة
// 1000 = ثانية واحدة
// 3000 = 3 ثوانٍ
```

---

### 3. إضافة قسم جديد

**مثال: إضافة قسم "المشاريع القادمة"**

**في `src/pages/index.tsx` - أضف قبل Footer:**

```typescript
{/* قسم المشاريع القادمة - جديد */}
<section id="upcoming" className="py-24 px-4 bg-white">
  <div className="container mx-auto">
    <ScrollReveal>
      <div className="text-center mb-16">
        <h2 className="text-5xl font-black text-gray-900 mb-6">
          المشاريع القادمة
        </h2>
        <p className="text-xl text-gray-600">
          نخطط لبناء المزيد من المساجد قريباً
        </p>
      </div>
    </ScrollReveal>

    <div className="grid md:grid-cols-3 gap-8">
      {/* بطاقات المشاريع */}
      <Card>
        <CardContent className="p-6">
          <h3 className="text-2xl font-bold mb-4">مسجد الفاروق</h3>
          <p className="text-gray-600">سيتم افتتاحه في يناير 2026</p>
        </CardContent>
      </Card>
      
      {/* أضف المزيد من البطاقات */}
    </div>
  </div>
</section>
```

**ثم أضف رابط القسم في القائمة (Header):**

```typescript
// في السطر ~68
<nav className="hidden lg:flex items-center gap-8">
  {["الرئيسية", "برامجنا", "الأخبار", "المشاريع", "من نحن"].map((item, idx) => (
    <a 
      href={`#${["home", "services", "news", "upcoming", "about"][idx]}`}
      ...
    >
      {item}
    </a>
  ))}
</nav>
```

---

### 4. تعديل تصميم البطاقات

**تغيير شكل بطاقات الخدمات:**

```typescript
// في src/pages/index.tsx - السطر ~240
<Card className="group ... bg-gradient-to-br from-white to-[#08704C]/5">
  // غير الألوان أو الشكل هنا
  
  // مثال: إضافة حدود سميكة:
  <Card className="... border-4 border-[#08704C]">
  
  // مثال: تغيير الظل:
  <Card className="... shadow-2xl hover:shadow-4xl">
</Card>
```

---

## 🔧 أوامر البناء والتطوير

### التطوير المحلي

```bash
# تشغيل المشروع في وضع التطوير
npm run dev

# سيعمل المشروع على:
# http://localhost:3000

# الميزات في وضع التطوير:
# ✅ تحديث تلقائي عند حفظ الملفات (Hot Reload)
# ✅ رسائل خطأ مفصلة
# ✅ أدوات تطوير React
```

### بناء المشروع للإنتاج

```bash
# 1. بناء المشروع
npm run build

# هذا الأمر:
# ✅ يحول TypeScript إلى JavaScript
# ✅ يحسّن الكود ويصغّره (Minify)
# ✅ يحسّن الصور والأصول
# ✅ ينشئ ملفات HTML ثابتة

# 2. اختبار النسخة المبنية محلياً
npm start

# سيعمل على:
# http://localhost:3000
```

---

## 🚀 نشر الموقع {#نشر-الموقع}

### 1. النشر على Vercel (موصى به) ⭐

**Vercel** هي الشركة التي طورت Next.js - أفضل خيار!

**خطوات النشر:**

**أ) عبر واجهة Softgen:**

1. اضغط على زر "Publish" في الواجهة
2. سجل دخول أو أنشئ حساب Vercel
3. اختر اسم المشروع
4. اضغط "Deploy"
5. انتظر 2-3 دقائق
6. ✅ موقعك جاهز!

**ب) عبر GitHub + Vercel:**

```bash
# 1. ارفع المشروع على GitHub
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/username/yaamur-website.git
git push -u origin main

# 2. اربط مع Vercel:
# - اذهب إلى vercel.com
# - سجل دخول بحساب GitHub
# - اضغط "New Project"
# - اختر المشروع من GitHub
# - اضغط "Deploy"
```

**الرابط النهائي سيكون:**
```
https://yaamur-website.vercel.app
```

---

### 2. النشر على Netlify

```bash
# 1. ثبت Netlify CLI
npm install -g netlify-cli

# 2. سجل دخول
netlify login

# 3. ابنِ المشروع
npm run build

# 4. انشر
netlify deploy --prod
```

---

### 3. ربط دومين مخصص

**بعد النشر على Vercel/Netlify:**

1. اشترِ دومين (مثل: yaamur.org)
2. في لوحة تحكم Vercel:
   - اذهب إلى "Settings" → "Domains"
   - أضف دومينك
   - اتبع التعليمات لتحديث DNS

**إعدادات DNS:**

```
Type    Name    Value
A       @       76.76.21.21
CNAME   www     cname.vercel-dns.com
```

---

## 🐛 حل المشاكل الشائعة {#حل-المشاكل}

### المشكلة 1: الموقع لا يعمل بعد `npm run dev`

**الحل:**

```bash
# 1. احذف node_modules و package-lock.json
rm -rf node_modules package-lock.json

# 2. أعد التثبيت
npm install

# 3. شغل المشروع
npm run dev
```

---

### المشكلة 2: الصور لا تظهر

**السبب:** مسارات الصور خاطئة

**الحل:**

```typescript
// ❌ خطأ
<img src="images/mosque.jpg" />

// ✅ صحيح (ابدأ بـ /)
<img src="/images/mosque.jpg" />

// تأكد أن الصورة موجودة في:
// public/images/mosque.jpg
```

---

### المشكلة 3: الخطوط لا تظهر بشكل صحيح

**الحل:**

```typescript
// تأكد من وجود dir="rtl" في _document.tsx
<Html lang="ar" dir="rtl">

// تأكد من تحميل الخطوط في _document.tsx
<link href="https://fonts.googleapis.com/css2?family=Cairo..." />

// تأكد من الإعدادات في tailwind.config.ts
fontFamily: {
  sans: ["Cairo", "sans-serif"],
}
```

---

### المشكلة 4: أخطاء TypeScript

**الحل:**

```bash
# تجاهل أخطاء TypeScript مؤقتاً (غير موصى به)
# في next.config.mjs:
typescript: {
  ignoreBuildErrors: true,
}

# أو: إصلاح الأخطاء (موصى به)
npm run lint
```

---

### المشكلة 5: الموقع بطيء

**الحلول:**

1. **تحسين الصور:**
```typescript
// استخدم مكون Image من Next.js
import Image from 'next/image';

<Image 
  src="/images/mosque.jpg" 
  alt="مسجد"
  width={800}
  height={600}
  quality={75}  // 50-75 للويب
/>
```

2. **تقليل حجم الصور:**
```bash
# استخدم أدوات ضغط الصور
# TinyPNG.com
# Squoosh.app
```

3. **استخدام WebP:**
```typescript
<Image src="/images/mosque.webp" ... />
// WebP أخف بـ 25-35% من JPEG
```

---

### المشكلة 6: واتساب لا يفتح بشكل صحيح

**الحل:**

```typescript
// تأكد من تنسيق الرقم الصحيح
const phoneNumber = "966555123456";  // ✅ صحيح
const phoneNumber = "+966555123456"; // ❌ خطأ (لا تضف +)
const phoneNumber = "0555123456";    // ❌ خطأ (بدون 966)

// اختبر الرابط:
https://wa.me/966555123456?text=مرحبا
```

---

## 📚 موارد مفيدة

### وثائق التقنيات

- [Next.js Docs](https://nextjs.org/docs) - وثائق Next.js
- [React Docs](https://react.dev) - وثائق React
- [Tailwind CSS](https://tailwindcss.com/docs) - وثائق Tailwind
- [Shadcn/UI](https://ui.shadcn.com) - مكتبة المكونات
- [Lucide Icons](https://lucide.dev) - الأيقونات

### أدوات مفيدة

- [Coolors.co](https://coolors.co) - اختيار ألوان
- [Google Fonts](https://fonts.google.com/?subset=arabic) - خطوط عربية
- [Unsplash](https://unsplash.com) - صور مجانية
- [TinyPNG](https://tinypng.com) - ضغط الصور
- [Can I Use](https://caniuse.com) - توافق المتصفحات

### قنوات تعليمية (بالعربية)

- [Elzero Web School](https://www.youtube.com/@ElzeroWebSchool) - دروس عربية
- [Codezilla](https://www.youtube.com/@Codezilla) - شروحات Next.js

---

## 💡 نصائح للمطورين

### أفضل الممارسات

1. **استخدم Git للتحكم بالإصدارات:**
```bash
git add .
git commit -m "وصف التغيير"
git push
```

2. **اختبر التغييرات محلياً قبل النشر:**
```bash
npm run build
npm start
```

3. **احتفظ بنسخة احتياطية:**
```bash
# قبل تغييرات كبيرة
git branch backup-$(date +%Y%m%d)
```

4. **راقب أداء الموقع:**
- استخدم [PageSpeed Insights](https://pagespeed.web.dev)
- استخدم [GTmetrix](https://gtmetrix.com)

5. **اكتب تعليقات في الكود:**
```typescript
// ✅ جيد
// هذا الكود يحسب عدد المساجد
const mosqueCount = mosques.filter(m => m.isActive).length;

// ❌ سيء (بدون تعليق)
const c = m.filter(x => x.a).length;
```

---

## 🎯 خطة العمل الموصى بها

### المرحلة 1: الإعداد الأولي (يوم 1)

- [ ] تثبيت المشروع وتشغيله محلياً
- [ ] فهم هيكل المشروع
- [ ] قراءة هذا الدليل كاملاً

### المرحلة 2: التخصيص الأساسي (يوم 2-3)

- [ ] تغيير الألوان حسب الهوية
- [ ] تحديث الشعار والصور
- [ ] تحديث النصوص والمحتوى
- [ ] إضافة معلومات الاتصال الحقيقية

### المرحلة 3: المحتوى (يوم 4-5)

- [ ] إضافة الأخبار الحقيقية
- [ ] تحديث أرقام الإنجازات
- [ ] إضافة شعارات الشركاء
- [ ] إضافة صور المساجد

### المرحلة 4: الاختبار (يوم 6)

- [ ] اختبار على الموبايل
- [ ] اختبار على أجهزة مختلفة
- [ ] اختبار جميع الروابط
- [ ] اختبار زر الواتساب

### المرحلة 5: النشر (يوم 7)

- [ ] بناء المشروع للإنتاج
- [ ] النشر على Vercel
- [ ] ربط الدومين المخصص
- [ ] اختبار الموقع المنشور

---

## 📞 الدعم والمساعدة

إذا واجهتك مشكلة:

1. **ابحث في هذا الدليل** - معظم الإجابات موجودة هنا
2. **راجع أخطاء المتصفح** - افتح Console (F12)
3. **راجع أخطاء الترمينال** - انظر للرسائل في CMD
4. **اسأل ChatGPT** - انسخ رسالة الخطأ واسأل
5. **اطلب مساعدة Softgen** - لو عطل فني في المنصة

---

## 🎉 الخلاصة

الآن لديك دليل شامل لكل شيء! 

**المفاتيح الأساسية:**

✅ **src/pages/index.tsx** - الصفحة الرئيسية (كل شيء هنا)
✅ **src/styles/globals.css** - الألوان والأنماط
✅ **src/components/** - المكونات القابلة للاستخدام
✅ **public/** - الصور والملفات العامة

**أوامر لا تنساها:**

```bash
npm run dev      # للتطوير المحلي
npm run build    # للبناء
npm start        # لاختبار النسخة المبنية
```

**نصيحة أخيرة:**
لا تخف من التجربة! يمكنك دائماً الرجوع للنسخة الأصلية عبر Git 🚀

---

**آخر تحديث:** نوفمبر 2025
**الإصدار:** 1.0
**المطور:** Softgen AI + جمعية يعمر

بالتوفيق في مشروعك! 🌟
